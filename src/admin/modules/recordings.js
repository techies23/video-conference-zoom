export const initRecordingsTable = () => {
    console.log('initRecordingsTable');
    const table = document.getElementById('vczapi_recordings_table');
    if (!table) return;

    console.log(table);

};